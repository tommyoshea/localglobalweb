# LocalGlobalWeb - Dublin Web Designers .htaccess
# Comprehensive SEO, Security, and Performance Configuration

# ===========================================
# HTTPS REDIRECTS & CANONICAL URLS
# ===========================================

# Force HTTPS and www for all domains
RewriteEngine On

# Redirect all HTTP to HTTPS
RewriteCond %{HTTPS} off
RewriteRule ^(.*)$ https://%{HTTP_HOST}%{REQUEST_URI} [L,R=301]

# Redirect non-www to www
RewriteCond %{HTTP_HOST} !^www\. [NC]
RewriteRule ^(.*)$ https://www.%{HTTP_HOST}%{REQUEST_URI} [L,R=301]

# Redirect specific domain variations to canonical
RewriteCond %{HTTP_HOST} ^localglobalweb\.com$ [NC]
RewriteRule ^(.*)$ https://www.localglobalweb.com/$1 [L,R=301]

RewriteCond %{HTTP_HOST} ^www\.localglobalweb\.com$ [NC]
RewriteCond %{HTTPS} off
RewriteRule ^(.*)$ https://www.localglobalweb.com/$1 [L,R=301]

# ===========================================
# SEO OPTIMIZATIONS
# ===========================================

# Remove trailing slashes
RewriteCond %{REQUEST_FILENAME} !-d
RewriteCond %{THE_REQUEST} /+[^?\s]*//
RewriteRule ^(.+)/$ /$1 [R=301,L]

# Remove .html extensions
RewriteCond %{THE_REQUEST} /+[^?\s]*\.html[\s?] [NC]
RewriteRule ^(.+)\.html$ /$1 [R=301,L]

# Internal rewrite for .html files
RewriteCond %{REQUEST_FILENAME} !-d
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME}.html -f
RewriteRule ^(.+)$ $1.html [L]

# ===========================================
# SECURITY HEADERS
# ===========================================

# Content Security Policy
<IfModule mod_headers.c>
    Header always set Content-Security-Policy "default-src 'self'; script-src 'self' 'unsafe-inline' 'unsafe-eval' https://www.googletagmanager.com https://www.google-analytics.com; style-src 'self' 'unsafe-inline' https://fonts.googleapis.com; font-src 'self' https://fonts.gstatic.com; img-src 'self' data: https:; connect-src 'self' https://www.google-analytics.com; frame-src 'self'; object-src 'none'; base-uri 'self'; form-action 'self';"
    
    # XSS Protection
    Header always set X-XSS-Protection "1; mode=block"
    
    # Prevent MIME type sniffing
    Header always set X-Content-Type-Options "nosniff"
    
    # Frame options
    Header always set X-Frame-Options "SAMEORIGIN"
    
    # Referrer Policy
    Header always set Referrer-Policy "strict-origin-when-cross-origin"
    
    # Permissions Policy
    Header always set Permissions-Policy "geolocation=(), microphone=(), camera=()"
    
    # Remove server signature
    Header always unset Server
    Header always unset X-Powered-By
</IfModule>

# ===========================================
# PERFORMANCE OPTIMIZATIONS
# ===========================================

# Enable Gzip compression
<IfModule mod_deflate.c>
    AddOutputFilterByType DEFLATE text/plain
    AddOutputFilterByType DEFLATE text/html
    AddOutputFilterByType DEFLATE text/xml
    AddOutputFilterByType DEFLATE text/css
    AddOutputFilterByType DEFLATE application/xml
    AddOutputFilterByType DEFLATE application/xhtml+xml
    AddOutputFilterByType DEFLATE application/rss+xml
    AddOutputFilterByType DEFLATE application/javascript
    AddOutputFilterByType DEFLATE application/x-javascript
    AddOutputFilterByType DEFLATE application/json
    AddOutputFilterByType DEFLATE image/svg+xml
</IfModule>

# Browser caching
<IfModule mod_expires.c>
    ExpiresActive On
    
    # Default expiration: 1 month
    ExpiresDefault "access plus 1 month"
    
    # HTML: 1 hour
    ExpiresByType text/html "access plus 1 hour"
    
    # CSS and JavaScript: 1 year
    ExpiresByType text/css "access plus 1 year"
    ExpiresByType application/javascript "access plus 1 year"
    ExpiresByType application/x-javascript "access plus 1 year"
    
    # Images: 1 year
    ExpiresByType image/jpg "access plus 1 year"
    ExpiresByType image/jpeg "access plus 1 year"
    ExpiresByType image/gif "access plus 1 year"
    ExpiresByType image/png "access plus 1 year"
    ExpiresByType image/webp "access plus 1 year"
    ExpiresByType image/svg+xml "access plus 1 year"
    ExpiresByType image/x-icon "access plus 1 year"
    
    # Fonts: 1 year
    ExpiresByType font/woff "access plus 1 year"
    ExpiresByType font/woff2 "access plus 1 year"
    ExpiresByType application/font-woff "access plus 1 year"
    ExpiresByType application/font-woff2 "access plus 1 year"
    
    # PDF: 1 month
    ExpiresByType application/pdf "access plus 1 month"
    
    # XML: 1 hour
    ExpiresByType application/xml "access plus 1 hour"
    ExpiresByType text/xml "access plus 1 hour"
</IfModule>

# ===========================================
# CUSTOM ERROR PAGES
# ===========================================

# Custom 404 page
ErrorDocument 404 /404.html

# Custom 500 page
ErrorDocument 500 /500.html

# ===========================================
# BLOCK MALICIOUS REQUESTS
# ===========================================

# Block bad bots
RewriteCond %{HTTP_USER_AGENT} ^$ [OR]
RewriteCond %{HTTP_USER_AGENT} ^(java|curl|wget).* [NC,OR]
RewriteCond %{HTTP_USER_AGENT} ^.*(libwww-perl|curl|wget|python|nikto|scan).* [NC,OR]
RewriteCond %{HTTP_USER_AGENT} ^.*(winhttp|HTTrack|clshttp|archiver|loader|email|harvest|extract|grab|miner).* [NC]
RewriteRule .* - [F,L]

# Block access to sensitive files
<FilesMatch "\.(htaccess|htpasswd|ini|log|sh|sql|conf)$">
    Order Allow,Deny
    Deny from all
</FilesMatch>

# Block access to backup files
<FilesMatch "\.(bak|backup|old|orig|save|swp|tmp)$">
    Order Allow,Deny
    Deny from all
</FilesMatch>

# ===========================================
# WORDPRESS SECURITY (if using WordPress)
# ===========================================

# Block access to wp-config.php
<Files wp-config.php>
    Order Allow,Deny
    Deny from all
</Files>

# Block access to wp-admin directory (if not needed)
# <Directory wp-admin>
#     Order Allow,Deny
#     Deny from all
# </Directory>

# ===========================================
# SEO FRIENDLY URLS
# ===========================================

# Redirect old URLs to new ones (if any)
# RewriteRule ^old-page\.html$ /new-page [R=301,L]

# ===========================================
# MOBILE OPTIMIZATION
# ===========================================

# Force mobile viewport
<IfModule mod_headers.c>
    <FilesMatch "\.(html|htm)$">
        Header always set X-UA-Compatible "IE=edge"
    </FilesMatch>
</IfModule>

# ===========================================
# ANALYTICS & TRACKING
# ===========================================

# Allow Google Analytics
<IfModule mod_headers.c>
    Header always set Access-Control-Allow-Origin "https://www.google-analytics.com"
</IfModule>

# ===========================================
# MAINTENANCE MODE (uncomment when needed)
# ===========================================

# RewriteCond %{REMOTE_ADDR} !^YOUR_IP_ADDRESS$
# RewriteCond %{REQUEST_URI} !^/maintenance\.html$
# RewriteRule .* /maintenance.html [R=302,L]

# ===========================================
# ADDITIONAL SECURITY
# ===========================================

# Prevent directory browsing
Options -Indexes

# Prevent access to .htaccess
<Files .htaccess>
    Order Allow,Deny
    Deny from all
</Files>

# ===========================================
# PERFORMANCE TUNING
# ===========================================

# Enable Keep-Alive
<IfModule mod_headers.c>
    Header always set Connection keep-alive
</IfModule>

# ===========================================
# DUBLIN WEB DESIGN SPECIFIC OPTIMIZATIONS
# ===========================================

# Cache static assets for Dublin web design site
<IfModule mod_expires.c>
    # Template images and assets
    ExpiresByType image/jpg "access plus 1 year"
    ExpiresByType image/jpeg "access plus 1 year"
    ExpiresByType image/png "access plus 1 year"
    ExpiresByType image/webp "access plus 1 year"
    
    # CSS and JS for web design templates
    ExpiresByType text/css "access plus 1 year"
    ExpiresByType application/javascript "access plus 1 year"
    
    # Fonts for web design
    ExpiresByType font/woff "access plus 1 year"
    ExpiresByType font/woff2 "access plus 1 year"
</IfModule>

# ===========================================
# END OF .HTACCESS FILE
# ===========================================

# This .htaccess file is optimized for LocalGlobalWeb Dublin Web Designers
# Last updated: 2024
# Contact: hello@localglobalweb.com
